server:
  port: 8081

spring:
  application:
    name: payment-service

payment:
  failure-rate: ${PAYMENT_FAILURE_RATE:0}

management:
  endpoints:
    web:
      exposure:
        include: health,info
